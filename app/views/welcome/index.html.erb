<% unless user_signed_in? %>

    <li>
      <%= form_for(:user, :url => session_path(:user)) do |f| %>
          <%= f.text_field :email %>
          <%= f.password_field :password %>
          <%= f.check_box :remember_me %>
          <%= f.label :remember_me %>
          <%= f.submit 'Sign in' %>
          <%= link_to "Forgot your password?", new_password_path(:user) %>
      <% end %>
    </li>
<% end %>

<% unless user_signed_in? %>
    <li>
    Not a member yet?  <%= link_to('Register now!', new_user_registration_path)  %>
    </li>
<% end %>

<h1>Welcome to Holiday Reminder!</h1>
<p>The title says it all. Remind your customers/friends of their favorite holidays. Start today!</p>
